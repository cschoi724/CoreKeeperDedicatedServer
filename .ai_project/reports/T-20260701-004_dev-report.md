# Dev Report: T-20260701-004

작성일: 2026-07-02
작성자: Development Agent
Task: GSM-R3 Config/Path Core 분리

## 결과

READY_FOR_QA

## 작업 범위

- `automation/src/Core/ConfigManager.psm1` 추가
- `automation/src/Core/PathManager.psm1` 추가
- `automation/src/CoreKeeper.Config.psm1` compatibility wrapper 전환
- `automation/src/CoreKeeper.Paths.psm1` compatibility wrapper 전환
- `automation/docs/MIGRATION_STRATEGY.md` 설정 우선순위 기록
- `automation/docs/STATUS.md`, `automation/docs/CHANGELOG.md`, `automation/docs/DEVELOPMENT_PLAN.md` 갱신

## 구현 내용

- 새 Core 설정 함수 `Get-GameServerSettings`가 Adapter manifest, manager 설정, game 설정, legacy 설정을 병합하도록 구현
- 새 Core 경로 함수 `Get-GameServerPathSet`가 Adapter의 `dataRoot`와 병합 설정을 기준으로 경로를 계산하도록 구현
- 기존 `Get-CKSettings`, `Get-CKPathSet`, `Initialize-CKRequiredDirectories`, `Test-CKRequiredPaths` 함수명 유지
- 기존 `settings.local.json`은 새 local 설정이 없을 때 Core Keeper 호환 설정으로 반영
- 경로 생성 함수는 서버 설치 경로, SteamCMD 경로, 백업 경로만 생성하며 사용자 데이터 경로 이동은 하지 않음

## 제외 범위

- SteamCMD 실행 로직 변경 없음
- 서버 실행/백업 로직 변경 없음
- 기존 설정 파일 삭제 없음

## 검증

macOS 환경에 `pwsh`와 `powershell`이 없어 Task의 PowerShell import 검증은 수행하지 못했다. 대신 가능한 정적 검증을 수행했다.

```bash
ruby -rjson -e 'ARGV.each { |path| JSON.parse(File.read(path)); puts "#{path}: OK" }' automation/config/manager.example.json automation/config/games/corekeeper.example.json automation/src/Games/CoreKeeper/game.json automation/config/settings.example.json
ruby -e 'ARGV.each { |p| text=File.read(p); abort("brace mismatch #{p}") unless text.count("{") == text.count("}"); abort("paren mismatch #{p}") unless text.count("(") == text.count(")"); puts "#{p}: structure OK" }' automation/src/Core/AdapterManager.psm1 automation/src/Core/ConfigManager.psm1 automation/src/Core/PathManager.psm1 automation/src/CoreKeeper.Config.psm1 automation/src/CoreKeeper.Paths.psm1
rg -n "Pugstorm|Core Keeper|1963720|CoreKeeper" automation/src/Core automation/src/CoreKeeper.Config.psm1 automation/src/CoreKeeper.Paths.psm1
git diff --check
```

결과:

- JSON 설정/manifest 파일 파싱 통과
- PowerShell 파일 중괄호/괄호 구조 확인 통과
- Core 모듈에는 Core Keeper 전용 상수 없음
- compatibility wrapper에는 wrapper 모듈명 `CoreKeeper.Config.psm1` 참조만 존재
- `git diff --check` 통과

## Windows 검증 필요

Windows 또는 PowerShell 설치 환경에서 아래 명령을 추가 확인해야 한다.

```powershell
Import-Module .\src\Core\ConfigManager.psm1 -Force
Import-Module .\src\Core\PathManager.psm1 -Force
Import-Module .\src\CoreKeeper.Config.psm1 -Force
Import-Module .\src\CoreKeeper.Paths.psm1 -Force
```

## 재작업 기록

작성일: 2026-07-02

- QA 지적에 따라 Core `PathManager.psm1`에서 Core Keeper 전용 `WorldsPath`, `WorldInfosPath`, `ServerConfigPath` 계산을 제거함
- 기존 Core Keeper 스크립트 호환을 위해 해당 경로 계산은 `CoreKeeper.Paths.psm1` wrapper의 `Get-CKPathSet`로 이동함
- `Initialize-CKRequiredDirectories`는 공통 디렉터리 초기화 후 Core Keeper wrapper 경로 세트를 반환하도록 조정함

재검증 결과:

- JSON 설정/manifest 파일 파싱 통과
- PowerShell 파일 중괄호/괄호 구조 확인 통과
- Core `PathManager.psm1`에서 `worlds`, `worldinfos`, `ServerConfig.json` 검색 결과 없음
- Core 모듈에서 `Pugstorm|Core Keeper|1963720|CoreKeeper` 검색 결과 없음
- `git diff --check` 통과
