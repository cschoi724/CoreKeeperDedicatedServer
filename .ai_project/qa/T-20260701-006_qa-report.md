# QA Report

작성일: 2026-07-02
작성 Agent: QA Agent
대상 Task ID: T-20260701-006
대상 Task: GSM-R5 Server/Backup/Scheduler Core 분리
Workflow: feature
Active Agents: PM Agent, Development Agent, QA Agent
Active Capabilities: qa_review, risk_review, technical_review
QA 결과: PASS_WITH_RISK

## Source Task

```text
.ai_project/tasks/T-20260701-006_gsm-r5-server-backup-scheduler-core.md
```

## 확인 범위

- `automation/src/Core/ServerManager.psm1`
- `automation/src/Core/BackupManager.psm1`
- `automation/src/Core/SchedulerManager.psm1`
- `automation/src/CoreKeeper.Server.psm1`
- `automation/src/CoreKeeper.Backup.psm1`
- `automation/src/CoreKeeper.Tasks.psm1`
- `automation/scripts/start-server.ps1`
- `automation/scripts/stop-server.ps1`
- `automation/scripts/status-server.ps1`
- `automation/scripts/backup-server.ps1`
- `automation/scripts/register-task.ps1`
- `automation/scripts/unregister-task.ps1`
- `automation/scripts/enable-task.ps1`
- `automation/scripts/disable-task.ps1`
- `automation/scripts/register-restart-task.ps1`
- `automation/scripts/unregister-restart-task.ps1`
- `.ai_project/reports/T-20260701-006_dev-report.md`

## 확인 결과

- Core Server/Backup/Scheduler 모듈은 추가되었고 정적 괄호/중괄호 구조 검증은 통과했다.
- Core Server/Backup/Scheduler 모듈에서 `Pugstorm`, `Core Keeper`, `1963720`, `CoreKeeper`, `worlds`, `worldinfos`, `ServerConfig.json` 전용 상수 검색 결과는 없다.
- 백업 대상은 `Settings["manifest"]["backup"]["targets"]`에서 읽고, 백업 동작은 `Copy-Item` 중심이며 원본 삭제/이동은 보이지 않는다.
- `stop-server.ps1`와 restart scheduler 경로에는 안전 종료 미검증 경고가 유지되어 있다.
- Task Scheduler 등록 실패 메시지는 관리자 권한 필요 가능성을 안내한다.
- 재검증 결과, 기존 Core Keeper server wrapper의 `CoreKeeper.Paths.psm1` import 누락은 수정되었다.

## 발견 이슈

| 심각도 | 파일/영역 | 내용 | 권장 조치 |
|---|---|---|---|
| Resolved | `automation/src/CoreKeeper.Server.psm1` | `Get-CKServerStatus`가 호출하는 `Get-CKPathSet` 의존성을 위해 `CoreKeeper.Paths.psm1` import가 추가되었다. | 추가 조치 없음 |
| Medium | PowerShell 실행 검증 | 현재 macOS 환경에 `pwsh`/`powershell`이 없어 Task 원 검증 명령인 `status-server.ps1`, `backup-server.ps1 -WhatIf`, `register-task.ps1 -WhatIf`를 실행하지 못했다. | 재작업 후 Windows 또는 PowerShell 환경에서 원 검증 명령을 실행한다. |
| Low | `-WhatIf` dry-run 범위 | Backup/Scheduler Core 함수는 Windows guard를 먼저 실행하므로, macOS PowerShell 7이 있더라도 `-WhatIf` preview는 Windows guard에서 중단된다. | 의도된 정책이면 유지한다. macOS dry-run preview가 필요하면 별도 Task로 분리한다. |
| Low | Task routing metadata | Task는 `ready_for_qa`였지만 `target_agent: Development Agent`로 남아 있었다. 사용자 지시에 따라 QA를 진행했으나 `.ai/task_queue.md`의 엄격 조건과는 불일치한다. | PM Agent가 후속 Task 템플릿 또는 상태 전이 규칙에서 QA 라우팅 메타데이터 처리 방식을 정리한다. |

## 문서 일치성

- Core 모듈의 Adapter 기반 실행 후보, 프로세스 패턴, 로그 힌트, 백업 대상 사용 방향은 R5 목표와 일치한다.
- 기존 Core Keeper server wrapper의 `Get-CKPathSet` 의존성은 `CoreKeeper.Paths.psm1` import 추가로 해소되었다.
- 안전 종료 미검증 경고와 관리자 권한 안내는 QA 확인 항목과 일치한다.

## 검증 결과

| 명령/항목 | 결과 | 비고 |
|---|---|---|
| `command -v pwsh` | FAIL | 현재 환경에 PowerShell 없음 |
| `command -v powershell` | FAIL | 현재 환경에 Windows PowerShell 없음 |
| PowerShell 파일 중괄호/괄호 구조 확인 | PASS | 단순 정적 확인 |
| Core Server/Backup/Scheduler 전용 상수 검색 | PASS | 검색 결과 없음 |
| wrapper 의존성 정적 검토 | PASS | `CoreKeeper.Server.psm1`에서 `CoreKeeper.Paths.psm1` import와 `Get-CKPathSet` 호출 확인 |
| `git diff --check` | PASS | 공백 오류 없음 |

## 잔여 리스크

- 실제 PowerShell import와 `-WhatIf` 실행은 현재 환경에서 검증하지 못했다.
- Windows Task Scheduler cmdlet 동작은 Windows 실기 검증이 필요하다.
- 백업은 복사 중심으로 보이나, 실제 파일 권한/잠금 상태에서의 동작은 Windows 실기 검증 전까지 미확정이다.

## PM Agent 권장 조치

- 커밋 가능 여부: 가능
- 재작업 필요 여부: 없음
- 사용자 결정 필요 여부: 없음
- 후속 권장: Windows 또는 PowerShell 환경에서 `status-server.ps1`, `backup-server.ps1 -WhatIf`, `register-task.ps1 -WhatIf` 원 검증 명령을 실행
