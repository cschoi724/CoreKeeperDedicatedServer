# QA Report: T-20260702-001

작성일: 2026-07-02
작성자: QA Agent
Task: GSM-R2 AdapterManager PowerShell import 검증

## 결과

PASS_WITH_RISK

## 검증 범위

- `.ai_project/reports/T-20260702-001_dev-report.md`
- `automation/src/Core/AdapterManager.psm1`
- `automation/src/Games/CoreKeeper/game.json`

## 확인 결과

- 현재 macOS 환경에 `pwsh`, `powershell`이 없어 Task 원 검증 명령은 실행하지 못했다.
- Dev Report는 PowerShell 부재와 Windows/PowerShell 환경 추가 검증 필요 상태를 명확히 기록했다.
- `AdapterManager.psm1`에는 `Get-GameServerAdapter`, `Get-GameServerAdapterList`, `Export-ModuleMember`가 존재한다.
- Core Keeper Adapter manifest는 JSON parse를 통과했고, AdapterManager가 요구하는 주요 필드를 포함한다.
- 구현 변경은 없었고, `git diff --check`는 통과했다.

## 수행한 검증

```bash
command -v pwsh
command -v powershell
ruby -rjson -e 'ARGV.each { |p| JSON.parse(File.read(p)); puts "#{p}: JSON OK" }' automation/src/Games/CoreKeeper/game.json
ruby -e 'ARGV.each { |p| text=File.read(p); abort("brace mismatch #{p}") unless text.count("{") == text.count("}"); abort("paren mismatch #{p}") unless text.count("(") == text.count(")"); puts "#{p}: structure OK" }' automation/src/Core/AdapterManager.psm1
rg -n 'function Get-GameServerAdapter|function Get-GameServerAdapterList|Export-ModuleMember|Assert-GSMAdapterManifest|Resolve-GSMAdapterManifestPath' automation/src/Core/AdapterManager.psm1
rg -n '"gameId"|"displayName"|"steam"|"paths"|"server"|"backup"|"features"|"appId"|"login"|"launchCandidates"|"processNamePatterns"|"targets"' automation/src/Games/CoreKeeper/game.json
git diff --check
```

## 미실행 원 검증

아래 PowerShell 검증은 현재 환경에 PowerShell이 없어 미실행이다.

```powershell
Import-Module .\src\Core\AdapterManager.psm1 -Force
Get-GameServerAdapter -Game corekeeper
Get-GameServerAdapterList
```

## 잔여 리스크

- Windows PowerShell 5.1 또는 `pwsh` 설치 환경에서 실제 import와 함수 호출 검증이 필요하다.
- PowerShell 런타임에서만 드러나는 module import, hashtable 변환, path resolution 문제는 현재 정적 검증만으로 완전히 배제할 수 없다.

## 결론

현재 환경에서 가능한 검증과 보고 조건은 충족한다. 원 PowerShell 검증 미실행 리스크를 명시한 상태로 `qa_passed` 처리 가능하다.
