# Dev Report: T-20260701-008

작성일: 2026-07-02
작성자: Development Agent
Task: GSM-R7 테스트와 Windows Runbook 재정렬

## 결과

READY_FOR_QA

## 작업 범위

- `automation/README.md`를 Steam Game Server Manager 기준으로 재작성
- `automation/docs/TESTING.md`를 공통 Core 검증과 Core Keeper Adapter 회귀 검증으로 분리
- `automation/docs/WINDOWS_CODEX_RUNBOOK.md`를 `-Game corekeeper` 기준 Windows 검증 절차로 갱신
- `automation/docs/STATUS.md`, `automation/docs/CHANGELOG.md`에 R7 진행 상태 기록

## 구현 내용

- Core Keeper 전용 자동화 템플릿 설명을 Steam Game Server Manager + `corekeeper` Adapter 설명으로 전환
- 설정 파일 기준을 `manager.local.json`, `games/corekeeper.local.json`, 기존 `settings.local.json` fallback으로 정리
- Windows 검증 절차를 공통 Core 모듈 import, Core Keeper compatibility wrapper, SteamCMD, 서버 실행, 백업, 월드 import, Task Scheduler 순서로 재정렬
- T10 증거 수집 항목을 기존 이름은 유지하되 공통 Core와 Core Keeper Adapter 회귀 검증 결과로 나누어 기록하도록 갱신
- macOS에서 수행하지 않는 검증 항목을 명확히 분리

## 제외 범위

- 기능 구현 없음
- Windows 실기 검증 직접 수행 없음
- 두 번째 게임 Adapter 추가 없음

## 검증

```bash
rg -n "Core Keeper 전용|T10|Steam Game Server Manager|Adapter" automation/README.md automation/docs/TESTING.md automation/docs/WINDOWS_CODEX_RUNBOOK.md
rg --pcre2 -n "install-server\\.ps1(?! -Game)|update-server\\.ps1(?! -Game)|start-server\\.ps1(?! -Game)|status-server\\.ps1(?! -Game)|backup-server\\.ps1(?! -Game)|import-world\\.ps1(?! -Game)|register-task\\.ps1(?! -Game)|register-restart-task\\.ps1(?! -Game)" automation/README.md automation/docs/TESTING.md automation/docs/WINDOWS_CODEX_RUNBOOK.md
rg -n "Get-GameServerTaskDefinition|New-CKSteamCmdAppUpdateArguments|T10 실기 검증|Core Keeper Dedicated Server Automation" automation/README.md automation/docs/TESTING.md automation/docs/WINDOWS_CODEX_RUNBOOK.md
git diff --check
```

결과:

- Task 검증 기준 문자열 검색 통과
- 주요 스크립트 예제에서 `-Game` 누락 패턴 없음
- 제거 대상 오래된 함수명/제목 검색 결과 없음
- `git diff --check` 통과

## Windows 검증 필요

문서가 안내하는 PowerShell 명령은 Windows 환경에서 실제 실행 검증이 필요하다. 현재 macOS 환경에는 `pwsh`가 없어 PowerShell import 검증은 수행하지 않았다.

## 재작업 기록

작성일: 2026-07-02

- QA 지적에 따라 `New-GameServerSteamCmdAppUpdateArguments -Game corekeeper` 문서 명령을 실제 함수 계약에 맞게 수정함
- `automation/docs/TESTING.md`, `automation/docs/WINDOWS_CODEX_RUNBOOK.md`에서 `$settings = Get-GameServerSettings -Game corekeeper` 후 `New-GameServerSteamCmdAppUpdateArguments -Settings $settings`를 호출하도록 변경함

재검증 결과:

- `rg -n "New-GameServerSteamCmdAppUpdateArguments -Game" automation/docs` 검색 결과 없음
- `rg -n "New-GameServerSteamCmdAppUpdateArguments" automation/docs/TESTING.md automation/docs/WINDOWS_CODEX_RUNBOOK.md automation/src`로 문서와 실제 함수 계약 대조 완료
- 주요 스크립트 예제의 `-Game` 누락 패턴 검색 결과 없음
- `git diff --check` 통과
