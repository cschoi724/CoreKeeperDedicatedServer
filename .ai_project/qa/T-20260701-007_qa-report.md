# QA Report: T-20260701-007

작성일: 2026-07-02
작성자: QA Agent
Task: GSM-R6 Core Keeper 월드 import Adapter 격리

## 결과

PASS_WITH_RISK

## 검증 범위

- `automation/src/Games/CoreKeeper/CoreKeeper.Adapter.psm1`
- `automation/src/CoreKeeper.World.psm1`
- `automation/scripts/import-world.ps1`
- 관련 문서의 검증 명령과 주의사항

## 확인 결과

- Core Keeper 전용 `.world.gzip` 검증은 Adapter의 `Test-CoreKeeperWorldFile`에 위치한다.
- `ServerConfig.json` world index 후보 필드 패치 로직은 Adapter의 `Update-GameServerConfig`에 위치한다.
- `CoreKeeper.World.psm1`는 Adapter manifest의 `features.worldImport`를 확인한 뒤 Adapter 모듈의 `Import-GameServerWorld`에 위임한다.
- `import-world.ps1`는 `-Game corekeeper` 기본값과 `-WhatIf` 전달 경로를 제공한다.
- import 전 `Backup-CKDedicatedServerData -Reason before-import` 호출이 유지된다.
- 대상 월드 파일이 이미 있으면 `-ConfirmOverwrite` 없이는 중단한다.
- 원본 월드 파일에는 `Copy-Item`만 사용하며 `Remove-Item` 또는 `Move-Item` 사용이 없다.

## 수행한 검증

```bash
command -v pwsh
command -v powershell
ruby -e 'ARGV.each { |p| text=File.read(p); abort("brace mismatch #{p}") unless text.count("{") == text.count("}"); abort("paren mismatch #{p}") unless text.count("(") == text.count(")"); puts "#{p}: structure OK" }' automation/src/Games/CoreKeeper/CoreKeeper.Adapter.psm1 automation/src/CoreKeeper.World.psm1 automation/scripts/import-world.ps1
rg -n "\\.world\\.gzip|ServerConfig\\.json" automation/src/Core automation/src/CoreKeeper.World.psm1 automation/scripts/import-world.ps1
rg -n "\\.world\\.gzip|ServerConfig\\.json|worldIndex|worldSlot|worldSlotIndex|worldId|worldID|Backup-CKDedicatedServerData|ConfirmOverwrite|Copy-Item|Remove-Item|Move-Item" automation/src/Games/CoreKeeper/CoreKeeper.Adapter.psm1 automation/src/CoreKeeper.World.psm1 automation/scripts/import-world.ps1
rg -n "Remove-Item|Move-Item|Set-Content|Copy-Item" automation/src/Games/CoreKeeper/CoreKeeper.Adapter.psm1 automation/src/CoreKeeper.World.psm1 automation/scripts/import-world.ps1
git diff --check
```

## 검증 결과

- `pwsh`, `powershell` 모두 현재 macOS 환경에 없어 원 PowerShell 명령은 실행하지 못했다.
- PowerShell 파일의 중괄호/괄호 구조 검사는 통과했다.
- `automation/src/Core`, `automation/src/CoreKeeper.World.psm1`, `automation/scripts/import-world.ps1`에서 `.world.gzip`/`ServerConfig.json` 규칙 검색 결과가 없었다.
- Core Keeper Adapter 내부에서만 `.world.gzip`, `ServerConfig.json`, world index 후보 필드 규칙이 확인됐다.
- 원본 월드 파일에 대한 삭제/이동 명령은 확인되지 않았고, 대상 경로로의 `Copy-Item`만 확인됐다.
- `git diff --check` 통과.

## 잔여 리스크

- Windows 또는 PowerShell 설치 환경이 없어 아래 Task 원 검증 명령은 미실행 상태다.

```powershell
.\scripts\import-world.ps1 -Game corekeeper -WorldFile "D:\Incoming\0.world.gzip" -WorldIndex 0 -WhatIf
```

- 실제 `ServerConfig.json`의 현재 스키마와 world index 필드명은 실행 환경에서 추가 확인이 필요하다. 단, 미확인 필드일 때 경고 후 변경하지 않는 방어 로직은 존재한다.
- `worldinfos` 자동 생성은 Task 제외 범위이므로 검증하지 않았다.
- Task 메타데이터의 `target_agent`는 Development Agent로 유지되어 있으나, 사용자 직접 지시로 QA 검증을 수행했다.

## 결론

재작업 요청 사항은 없다. Windows/PowerShell 환경에서 원 검증 명령을 추가 수행하는 조건으로 `qa_passed` 처리 가능하다.
