# Dev Report: T-20260701-006

작성일: 2026-07-02
작성자: Development Agent
Task: GSM-R5 Server/Backup/Scheduler Core 분리

## 결과

READY_FOR_QA

## 작업 범위

- `automation/src/Core/ServerManager.psm1` 추가
- `automation/src/Core/BackupManager.psm1` 추가
- `automation/src/Core/SchedulerManager.psm1` 추가
- `automation/src/CoreKeeper.Server.psm1` wrapper 전환
- `automation/src/CoreKeeper.Backup.psm1` wrapper 전환
- `automation/src/CoreKeeper.Tasks.psm1` wrapper 전환
- `start/status/backup/task` 계열 스크립트에 `-Game corekeeper` 기본값 추가
- `backup/task` 계열 스크립트에 `-WhatIf` 전달 추가

## 구현 내용

- 서버 실행 후보와 프로세스 패턴을 Adapter manifest에서 읽도록 전환
- 상태 힌트 패턴을 Adapter manifest `logs.statusPatterns`에서 읽도록 전환
- 백업 대상을 Adapter manifest `backup.targets`에서 읽도록 전환
- Task Scheduler 작업명은 설정 병합 결과의 `taskName`, `restartTaskName`을 사용하도록 유지
- Task 설명과 실행 인자에 game ID를 포함
- 안전 종료 미검증 경고와 관리자 권한 안내 유지

## 제외 범위

- Core Keeper 월드 import 리팩터링 없음
- 안전 종료 자동화 확정 없음
- Watchdog 구현 없음

## 검증

macOS 환경에 `pwsh`가 없어 Task의 PowerShell 실행 검증은 수행하지 못했다. 대신 가능한 정적 검증을 수행했다.

```bash
ruby -e 'ARGV.each { |p| text=File.read(p); abort("brace mismatch #{p}") unless text.count("{") == text.count("}"); abort("paren mismatch #{p}") unless text.count("(") == text.count(")"); puts "#{p}: structure OK" }' automation/src/Core/ServerManager.psm1 automation/src/Core/BackupManager.psm1 automation/src/Core/SchedulerManager.psm1 automation/src/CoreKeeper.Server.psm1 automation/src/CoreKeeper.Backup.psm1 automation/src/CoreKeeper.Tasks.psm1 automation/scripts/start-server.ps1 automation/scripts/stop-server.ps1 automation/scripts/status-server.ps1 automation/scripts/backup-server.ps1 automation/scripts/register-task.ps1 automation/scripts/unregister-task.ps1 automation/scripts/enable-task.ps1 automation/scripts/disable-task.ps1 automation/scripts/register-restart-task.ps1 automation/scripts/unregister-restart-task.ps1
rg -n "Pugstorm|Core Keeper|1963720|CoreKeeper|worldinfos|ServerConfig\\.json|worlds" automation/src/Core/ServerManager.psm1 automation/src/Core/BackupManager.psm1 automation/src/Core/SchedulerManager.psm1
git diff --check
```

결과:

- PowerShell 파일 중괄호/괄호 구조 확인 통과
- Core Server/Backup/Scheduler 모듈에서 Core Keeper 전용 상수 검색 결과 없음
- `git diff --check` 통과

## Windows 검증 필요

Windows 또는 PowerShell 설치 환경에서 아래 명령을 추가 확인해야 한다.

```powershell
.\scripts\status-server.ps1 -Game corekeeper
.\scripts\backup-server.ps1 -Game corekeeper -WhatIf
.\scripts\register-task.ps1 -Game corekeeper -WhatIf
```

## 재작업 기록

작성일: 2026-07-02

- QA 지적에 따라 `CoreKeeper.Server.psm1`에서 `Get-CKPathSet` 호출에 필요한 `CoreKeeper.Paths.psm1` import를 추가함
- 기존 wrapper만 import한 사용자가 `Get-CKServerStatus`를 호출해도 wrapper 의존성이 충족되도록 정리함

재검증 결과:

- PowerShell 파일 중괄호/괄호 구조 확인 통과
- `CoreKeeper.Server.psm1`에서 `CoreKeeper.Paths.psm1` import와 `Get-CKPathSet` 호출을 함께 확인
- Core Server/Backup/Scheduler 모듈에서 Core Keeper 전용 상수 검색 결과 없음
- `git diff --check` 통과
