# QA Report

작성일: 2026-07-02
작성 Agent: QA Agent
대상 Task ID: T-20260701-004
대상 Task: GSM-R3 Config/Path Core 분리
Workflow: feature
Active Agents: PM Agent, Development Agent, QA Agent
Active Capabilities: qa_review, risk_review, technical_review
QA 결과: PASS_WITH_RISK

## Source Task

```text
.ai_project/tasks/T-20260701-004_gsm-r3-config-path-core.md
```

## 확인 범위

- `automation/src/Core/ConfigManager.psm1`
- `automation/src/Core/PathManager.psm1`
- `automation/src/CoreKeeper.Config.psm1`
- `automation/src/CoreKeeper.Paths.psm1`
- `automation/config/*.json`
- `automation/config/games/*.json`
- `automation/src/Games/CoreKeeper/game.json`
- `automation/docs/DEVELOPMENT_PLAN.md`
- `automation/docs/MIGRATION_STRATEGY.md`
- `.ai_project/reports/T-20260701-004_dev-report.md`

## 확인 결과

- `ConfigManager.psm1`, `PathManager.psm1`가 추가되었고 기존 `CoreKeeper.Config.psm1`, `CoreKeeper.Paths.psm1`는 wrapper 형태로 전환되었다.
- JSON 설정과 manifest 파일은 파싱에 성공했다.
- 기존 `settings.example.json`은 삭제되지 않았고, `settings.local.json` fallback 경로도 코드에 남아 있다.
- `Initialize-GameServerRequiredDirectories`는 서버 설치 경로, SteamCMD 경로, 백업 경로만 생성하며 사용자 데이터 경로 이동은 시도하지 않는다.
- 재검증 결과, Core 모듈인 `automation/src/Core/PathManager.psm1`에서는 Core Keeper 전용 데이터 구조인 `worlds`, `worldinfos`, `ServerConfig.json` 경로 계산이 제거되었다.
- Core Keeper 전용 경로 계산은 compatibility wrapper인 `automation/src/CoreKeeper.Paths.psm1`로 이동했다.

## 발견 이슈

| 심각도 | 파일/영역 | 내용 | 권장 조치 |
|---|---|---|---|
| Resolved | `automation/src/Core/PathManager.psm1`, `automation/src/CoreKeeper.Paths.psm1` | Core 모듈에 있던 `worlds`, `worldinfos`, `ServerConfig.json` 경로 계산이 제거되고 compatibility wrapper로 이동했다. | 추가 조치 없음 |
| Medium | PowerShell 실행 검증 | 현재 macOS 환경에 `pwsh`/`powershell`이 없어 Task 원 검증 명령인 4개 `Import-Module`을 실행하지 못했다. | 재작업 후 Windows 또는 PowerShell 환경에서 import 검증을 수행한다. |
| Low | 산출물 구조 | `DEVELOPMENT_PLAN.md`는 R3 산출물로 `automation/src/Compatibility/CoreKeeper.Legacy.psm1`를 적고 있으나 실제 구현은 기존 `CoreKeeper.Config.psm1`, `CoreKeeper.Paths.psm1` wrapper를 직접 수정하는 방식이다. Task allowed_paths에는 기존 wrapper 파일이 포함되어 있어 치명적 범위 위반은 아니다. | PM/Development가 현재 wrapper 유지 방식을 승인할지, 별도 Compatibility 폴더를 만들지 문서와 맞춘다. |
| Low | Task routing metadata | Task는 `ready_for_qa`였지만 `target_agent: Development Agent`로 남아 있었다. 사용자 지시에 따라 QA를 진행했으나 `.ai/task_queue.md`의 엄격 조건과는 불일치한다. | PM Agent가 후속 Task 템플릿 또는 상태 전이 규칙에서 QA 라우팅 메타데이터 처리 방식을 정리한다. |

## 문서 일치성

- 설정 fallback과 우선순위 문서화는 `MIGRATION_STRATEGY.md`와 대체로 일치한다.
- `DEVELOPMENT_PLAN.md`의 R3 목표와 Task 완료 조건은 Core Keeper 전용 경로 상수를 Core 밖으로 이동하도록 요구한다.
- 현재 `PathManager.psm1` 구현은 공통 경로만 반환하며, Core Keeper 백업/월드 구조는 wrapper 계층에 위치한다.

## 검증 결과

| 명령/항목 | 결과 | 비고 |
|---|---|---|
| `command -v pwsh` | FAIL | 현재 환경에 PowerShell 없음 |
| `command -v powershell` | FAIL | 현재 환경에 Windows PowerShell 없음 |
| JSON 파싱: manager/game/corekeeper/settings example | PASS | Ruby JSON 파서로 확인 |
| PowerShell 파일 중괄호/괄호 구조 확인 | PASS | 단순 정적 확인 |
| `rg -n "Pugstorm\|Core Keeper\|1963720\|CoreKeeper\|worldinfos\|ServerConfig\.json\|worlds" automation/src/Core` | PASS | Core 폴더 검색 결과 없음 |
| `rg -n "Pugstorm\|Core Keeper\|1963720\|CoreKeeper\|worldinfos\|ServerConfig\.json\|worlds" automation/src/Core automation/src/CoreKeeper.Config.psm1 automation/src/CoreKeeper.Paths.psm1` | PASS_WITH_RISK | Core Keeper wrapper에만 전용 경로 조각 존재 |
| `git diff --check` | PASS | 공백 오류 없음 |

## 잔여 리스크

- PowerShell 문법과 실제 모듈 import는 현재 환경에서 검증하지 못했다.
- 기존 wrapper 방식과 계획 문서의 `Compatibility/CoreKeeper.Legacy.psm1` 산출물 차이가 남아 있다.
- PowerShell import 검증은 아직 실제 실행되지 않았다.

## PM Agent 권장 조치

- 커밋 가능 여부: 가능
- 재작업 필요 여부: 없음
- 사용자 결정 필요 여부: 없음
- 후속 권장: Windows 또는 `pwsh` 환경에서 4개 `Import-Module` 명령을 실행해 실제 PowerShell import를 확인
