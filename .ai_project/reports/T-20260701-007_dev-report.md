# Dev Report: T-20260701-007

작성일: 2026-07-02
작성자: Development Agent
Task: GSM-R6 Core Keeper 월드 import Adapter 격리

## 결과

READY_FOR_QA

## 작업 범위

- `automation/src/Games/CoreKeeper/CoreKeeper.Adapter.psm1` 추가
- `automation/src/CoreKeeper.World.psm1`를 Adapter dispatcher/wrapper로 전환
- `automation/scripts/import-world.ps1`에 `-Game corekeeper` 기본값과 `-WhatIf` 전달 추가
- 관련 개발/상태/테스트/Runbook 문서 갱신

## 구현 내용

- Core Keeper 전용 `.world.gzip` 검증을 `Test-CoreKeeperWorldFile`로 이동
- Core Keeper 전용 서버 중지 확인을 `Assert-CoreKeeperDedicatedServerStopped`로 이동
- `ServerConfig.json` world index 후보 필드 패치 로직을 `Update-GameServerConfig`로 이동
- 실제 import 흐름을 `Import-GameServerWorld`로 이동
- `CoreKeeper.World.psm1`는 Adapter manifest의 `features.worldImport`를 확인하고 Adapter 모듈에 위임
- 지원하지 않는 게임은 `features.worldImport` 기준으로 unsupported 메시지를 출력
- import 전 `Backup-CKDedicatedServerData -Reason before-import` 호출 유지
- 대상 파일 존재 시 `-ConfirmOverwrite` 없이는 중단하는 정책 유지
- 원본 월드 파일은 읽기만 하고 삭제/이동하지 않음

## 제외 범위

- 다른 게임의 월드 import 구현 없음
- Core Keeper `worldinfos` 자동 생성 추측 구현 없음
- 안전 종료 자동화 없음

## 검증

macOS 환경에 `pwsh`가 없어 Task의 PowerShell 실행 검증은 수행하지 못했다. 대신 가능한 정적 검증을 수행했다.

```bash
ruby -e 'ARGV.each { |p| text=File.read(p); abort("brace mismatch #{p}") unless text.count("{") == text.count("}"); abort("paren mismatch #{p}") unless text.count("(") == text.count(")"); puts "#{p}: structure OK" }' automation/src/Games/CoreKeeper/CoreKeeper.Adapter.psm1 automation/src/CoreKeeper.World.psm1 automation/scripts/import-world.ps1
rg -n "\\.world\\.gzip|ServerConfig\\.json" automation/src/Core automation/src/CoreKeeper.World.psm1 automation/scripts/import-world.ps1
rg -n "\\.world\\.gzip|ServerConfig\\.json|worldIndex|worldSlot|worldId|worldID" automation/src/Games/CoreKeeper/CoreKeeper.Adapter.psm1
git diff --check
```

결과:

- PowerShell 파일 중괄호/괄호 구조 확인 통과
- 공통 Core, `CoreKeeper.World.psm1`, `import-world.ps1`에서 `.world.gzip`/`ServerConfig.json` 규칙 검색 결과 없음
- Core Keeper Adapter에서 `.world.gzip`, `ServerConfig.json`, world index 후보 필드 규칙 확인
- `git diff --check` 통과

## Windows 검증 필요

Windows 또는 PowerShell 설치 환경에서 아래 명령을 추가 확인해야 한다.

```powershell
.\scripts\import-world.ps1 -Game corekeeper -WorldFile "D:\Incoming\0.world.gzip" -WorldIndex 0 -WhatIf
```
